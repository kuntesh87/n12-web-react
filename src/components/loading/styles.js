import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
  mainWrapper: { textAlign: 'center', marginTop: 100, marginBottom: 100 },
}));

export default useStyles; 